//为tab底部添加1css像素的边框
//stylus形参前加$,less-@:
border-1px($color)
  position relative
  &::before//after一般用于在底部清除浮动
    position absolute
    bottom 0
    content ''
    width 100%
    height 1px
    background $color

bg-img($url)
  background-image url($url + '@2x.png')
  @media (-webkit-min-device-pixel-ratio: 3),(min-device-pixel-ratio: 3)
    background-image url($url+"@3x.png")

clear()
  zoom 1
  &:after//单：也行
    content ''
    display block
    clear both
